cmake-build-debug
.DS_Store
libromfs-generator
build*
dist/
vcpkg_installed
.psv-logs/
docs/_build/

# Editor / agent scratch
.cursor/

# Runtime state copied back from the SD card. These carry host names, LAN
# addresses, MACs and pairing certificates, so keep them unstageable even
# when they land outside dist/.
settings.json
!.vscode/settings.json
profile.json
artemis_*.json
*.pem
*.key

# Local VPN credentials. These hold a NetBird setup key / WireGuard private key
# and must never be committed.
/netbird_config.json
/netbird_switch_config.json
/netbird.conf
/wg0.conf
