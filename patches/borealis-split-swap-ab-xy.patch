diff --git a/library/include/borealis/core/application.hpp b/library/include/borealis/core/application.hpp
index 857aee54..3aff5279 100644
--- a/library/include/borealis/core/application.hpp
+++ b/library/include/borealis/core/application.hpp
@@ -344,11 +344,24 @@ class Application
         return debuggingViewEnabled;
     }
 
+    // Convenience: set both face-button pairs (A/B and X/Y) together.
     static void setSwapInputKeys(bool swap);
+    static void setSwapABInputKeys(bool swap);
+    static void setSwapXYInputKeys(bool swap);
+
+    inline static bool isSwapABInputKeys()
+    {
+        return swapABInputKeys;
+    }
+
+    inline static bool isSwapXYInputKeys()
+    {
+        return swapXYInputKeys;
+    }
 
     inline static bool isSwapInputKeys()
     {
-        return swapInputKeys;
+        return swapABInputKeys || swapXYInputKeys;
     }
 
     inline static void setDrawCoursor(bool draw)
@@ -394,7 +407,8 @@ class Application
     inline static bool inited               = false;
     inline static bool quitRequested        = false;
     inline static bool debuggingViewEnabled = false;
-    inline static bool swapInputKeys        = false;
+    inline static bool swapABInputKeys      = false;
+    inline static bool swapXYInputKeys      = false;
     inline static bool drawCoursor          = false;
     inline static bool swapHalfJoyconStickToDpad = false;
 
diff --git a/library/lib/core/application.cpp b/library/lib/core/application.cpp
index 5b2c9ef9..082c16d0 100644
--- a/library/lib/core/application.cpp
+++ b/library/lib/core/application.cpp
@@ -1175,9 +1175,22 @@ bool Application::isInputBlocks()
     return Application::blockInputsTokens > 0;
 }
 
+void Application::setSwapABInputKeys(bool swap)
+{
+    swapABInputKeys = swap;
+    getGlobalHintsUpdateEvent()->fire();
+}
+
+void Application::setSwapXYInputKeys(bool swap)
+{
+    swapXYInputKeys = swap;
+    getGlobalHintsUpdateEvent()->fire();
+}
+
 void Application::setSwapInputKeys(bool swap)
 {
-    swapInputKeys = swap;
+    swapABInputKeys = swap;
+    swapXYInputKeys = swap;
     getGlobalHintsUpdateEvent()->fire();
 }
 
diff --git a/library/lib/core/input.cpp b/library/lib/core/input.cpp
index b8dc580b..7cc4cb5e 100644
--- a/library/lib/core/input.cpp
+++ b/library/lib/core/input.cpp
@@ -68,19 +68,16 @@ MouseState InputManager::computeMouseState(RawMouseState currentTouch, MouseStat
 
 ControllerButton InputManager::mapControllerState(ControllerButton button)
 {
-    if (!Application::isSwapInputKeys())
-        return button;
-
     switch (button)
     {
         case BUTTON_A:
-            return BUTTON_B;
+            return Application::isSwapABInputKeys() ? BUTTON_B : BUTTON_A;
         case BUTTON_B:
-            return BUTTON_A;
+            return Application::isSwapABInputKeys() ? BUTTON_A : BUTTON_B;
         case BUTTON_X:
-            return BUTTON_Y;
+            return Application::isSwapXYInputKeys() ? BUTTON_Y : BUTTON_X;
         case BUTTON_Y:
-            return BUTTON_X;
+            return Application::isSwapXYInputKeys() ? BUTTON_X : BUTTON_Y;
         default:
             return button;
     }
diff --git a/library/lib/platforms/glfw/glfw_input.cpp b/library/lib/platforms/glfw/glfw_input.cpp
index 9b31fcfa..eec61375 100644
--- a/library/lib/platforms/glfw/glfw_input.cpp
+++ b/library/lib/platforms/glfw/glfw_input.cpp
@@ -264,7 +264,7 @@ void GLFWInputManager::updateUnifiedControllerState(ControllerState* state)
     }
 
     // Add keyboard keys on top of gamepad buttons
-    if (Application::isSwapInputKeys()) {
+    if (Application::isSwapABInputKeys()) {
         state->buttons[BUTTON_B] |= glfwGetKey(this->window, GLFW_KEY_KP_ENTER) != 0;
         state->buttons[BUTTON_B] |= glfwGetKey(this->window, GLFW_KEY_ENTER) != 0;
         state->buttons[BUTTON_A] |= glfwGetKey(this->window, GLFW_KEY_ESCAPE) != 0;
diff --git a/library/lib/platforms/sdl/sdl_input.cpp b/library/lib/platforms/sdl/sdl_input.cpp
index 031416ff..397fc634 100644
--- a/library/lib/platforms/sdl/sdl_input.cpp
+++ b/library/lib/platforms/sdl/sdl_input.cpp
@@ -577,7 +577,7 @@ void SDLInputManager::updateUnifiedControllerState(ControllerState* state)
     }
 
     // Add keyboard keys on top of gamepad buttons
-    if (Application::isSwapInputKeys())
+    if (Application::isSwapABInputKeys())
     {
         state->buttons[BUTTON_B] |= getKeyboardKeys(SDL_SCANCODE_KP_ENTER);
         state->buttons[BUTTON_B] |= getKeyboardKeys(SDL_SCANCODE_RETURN);
diff --git a/library/lib/platforms/switch/switch_input.cpp b/library/lib/platforms/switch/switch_input.cpp
index 195b4c35..6d93a9c4 100644
--- a/library/lib/platforms/switch/switch_input.cpp
+++ b/library/lib/platforms/switch/switch_input.cpp
@@ -199,7 +199,7 @@ void SwitchInputManager::updateUnifiedControllerState(ControllerState* state)
     state->buttons[BUTTON_NAV_DOWN] |= getKeyboardKeyState(BRLS_KBD_KEY_DOWN);
     state->buttons[BUTTON_NAV_LEFT] |= getKeyboardKeyState(BRLS_KBD_KEY_LEFT);
 
-    if (Application::isSwapInputKeys())
+    if (Application::isSwapABInputKeys())
     {
         state->buttons[BUTTON_B] |= getKeyboardKeyState(BRLS_KBD_KEY_ENTER);
         state->buttons[BUTTON_A] |= getKeyboardKeyState(BRLS_KBD_KEY_ESCAPE);
